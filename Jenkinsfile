pipeline {
    agent any

    stages {
        stage('Install') {
            steps {
                echo 'Installing Node modules'
            }
        }

        stage('Build') {
            steps {
                echo 'Building Node app'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing Node app'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying Node app'
            }
        }
    }
}
